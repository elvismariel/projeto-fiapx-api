# projeto-fiapx

## Pré-requisitos

Para rodar este projeto, você precisa ter o **ffmpeg** instalado no seu sistema.

### Instalação (Linux/Ubuntu/Pop!_OS)
```bash
sudo apt update && sudo apt install -y ffmpeg
```

### Verificação
```bash
ffmpeg -version
```
